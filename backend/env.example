# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
MONGODB_URI=mongodb://root:example@mongo:27017/connec?authSource=admin

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# CORS Configuration
# Comma-separated list of allowed origins (domains)
# Example: ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com,http://localhost:3000
ALLOWED_ORIGINS=http://localhost:5174,http://localhost:3000,https://yourdomain.com

# Frontend URL for CORS (legacy - kept for backward compatibility)
FRONTEND_URL=http://localhost:5174

# MongoDB Configuration (for docker-compose)
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD=example 